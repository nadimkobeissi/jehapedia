baseURL = 'https://jehapedia.org/'
languageCode = 'ar'
title = 'جِحَابيديا'
theme = 'alf-layla'
defaultContentLanguage = 'ar'

[taxonomies]
  tag = 'tags'

[params]
  description = 'موسوعة نوادر جحا'
  descriptionEn = 'The Jeha Encyclopedia'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[caches]
  [caches.images]
    dir = ':cacheDir/images'

[sitemap]
  changefreq = 'weekly'
  filename = 'sitemap.xml'
  priority = 0.5

[outputs]
  home = ['HTML', 'RSS', 'JSON']
  section = ['HTML', 'RSS']

[outputFormats]
  [outputFormats.RSS]
    mediaType = 'application/rss+xml'
    baseName = 'feed'
